. device
 . scan multiple time per step instead of doing multiple pass
 . receive commands from host

. host
 . commands sending (sync, scanning range, calibration...)

. process_dat
 . calibration, measurement
  . how long one step
  . how long is 1mv
   -> plot in a true unit system
 . averaging
  -> dont use arithmetic mean, variance, stddev
 . median filtering
  -> given kernel size
   -> approx 4 or 5 steps
  -> smooth he peaks ... (median filtering should do)
