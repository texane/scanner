. device
 . scan multiple time per step instead of doing multiple pass
 . receive commands from host

. host
 . commands sending (sync, scanning range, calibration...)

. process_dat
 . averaging
  -> dont use arithmetic mean, variance, stddev
 . median filtering
  -> given kernel size
  -> 
 . lissage des pics